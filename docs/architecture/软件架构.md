# 软件架构

> 状态：**未定**（依赖 `docs/design/总体方案.md` 的路线决策）。
> 平台：M3 Pro + **Jetson Orin NX**（箱内 3 块板中选，来源见 `hardware/平台硬件.md`）
> 软件栈：**ROS2** + OpenCV（【查资料】官方体系为 ROS2 + micro-ROS，拿到车需确认镜像）
> **算力明显过剩** —— 巡线 + HSV 色块 + DFS 用传统 OpenCV 即可，连 YOLO 都未必需要。

---

## 1. 模块划分（与方案解耦）

| 模块 | 职责 | 输入 | 输出 | 目录 |
|---|---|---|---|---|
| `perception` | 循迹偏移、路口检测、方块识别 | 图像 / 雷达 / IMU | 偏移误差、路口类型、方块位姿与颜色 | `software/src/` |
| `mapping` | 树形拓扑图维护 | 路口检测结果 + 里程计 | 当前节点、分支、已探索标记 | `software/src/` |
| `planner` | 决策下一目标、回程、兜底 | 拓扑图 + 收益模型参数 | 下一个节点/动作序列 | `software/src/` |
| `control` | 运动执行、循迹 PID、避障 | 目标方向与偏移 | 底盘速度指令 | `software/src/` |
| `manipulation` | 拾取/携带机构控制 | 拾取触发 | 机构动作 | `software/src/` |
| `sim` | 迷宫仿真与策略离线验证 | 地图 + 策略 | 时间/收集数指标 | `software/sim/` |
| `tools` | 标定、录包、回放 | — | — | `software/tools/` |

---

## 2. 设计约束

1. **罚时系数与地图现场公布** → 收益模型参数必须**外部可配置**（配置文件/启动参数），
   **禁止硬编码**在 planner 里
2. **模块间只通过明确话题/接口通信**，便于单独替换与调试
3. **每个模块可独立运行测试**（不依赖实车）：`sim` 提供仿真数据源
4. **必须有 watchdog**：卡死/超时 → 强制回出口

---

## 3. 状态机（建议）

```
INIT → 等待开始信号
  ↓
EXPLORE（沿通道循迹 + 路口检测 + 建图 + 识别方块）
  ↓（遇方块）
COLLECT（拾取）
  ↓
DECIDE（根据收益模型：继续深入 / 折返换分支 / 回出口）
  ↓
RETURN（沿父指针回出口）
  ↓
DONE（进入出口判定区，停表）
```
任何状态异常 → `RECOVER`（原地恢复/后退重试）→ 失败则转 `RETURN`。

---

## 4. 硬件/软件链路

```
顶部 RGB-D ─┐
双 TOF 雷达 ─┼→ Jetson Orin NX（ROS2 + OpenCV）→ micro-ROS/串口 → STM32 控制板 → 电机/机械臂
8路灰度 ─────┘        ↑                                              ↑
          （经小 MCU USB 串口）          你写的代码主要在这里            原厂固件，别动
```

> **原则：完全不动原厂底层控制固件。** 加传感器走"小 MCU → USB 串口 → ROS2 节点"，
> 不要为了接一个巡线模块把原车电机控制/micro-ROS 搞炸。

---

## 5. 待确认

- [x] ~~ROS 版本与平台已有的驱动/包~~ → ROS2（待实车确认镜像完整性）
- [x] ~~主控算力~~ → Orin NX，过剩，不成为瓶颈
- [x] ~~仿真工具选型~~ → **自写轻量 2D 网格仿真**（本题是 2D 网格迷宫，Gazebo 调试成本不划算）
- [ ] **Orin NX 是否已刷好官方 ROS 镜像/环境**（决定 9/20 能不能直接开机跑 demo）
- [ ] 官方例程覆盖度：底盘控制 / 摄像头 / RGB-D / 雷达 / 机械臂 / 视觉巡线 / 颜色识别
      → **有现成的就别重写**，这题最大的风险不是做不出来，而是官方已做好又重新造轮子
