# TODO

> 规则：这里只写**可执行的下一步动作**，不写计划复述。
> 每项必须能指派到人 + 有产出。完成即勾选，不做无声滚动。

基准日：2026-09-22 · 距提交 **48 天**

---

## ▶️ 现在做这个（就 3 步，按顺序来）

> **不要通读官方那 15 个资料目录。** 能提前提取的信息已经提完了，
> 结论都在 `docs/官方例程盘点.md` 里（哪些官方有、哪些要自己写）。
> 剩下的必须上手跑才知道。**看资料的时间到此为止，现在去跑车。**

### 第 0 步 · 确认车的基础环境（5 分钟，不确认后面全是瞎猜）

```bash
ssh car
source /opt/ros/humble/setup.bash
source ~/yahboomcar_ws/install/setup.bash
ros2 topic list
sh ~/start_agent.sh          # 通讯代理，很多例程要先起它
```

确认这三样在不在：**双雷达**（`/scan0` `/scan1`）· **相机**话题 · **`/cmd_vel`**（底盘）
→ 有缺的先解决，别往下走。

> **09-22 首次实测结果**（详见 `docs/车端实测记录.md`）：
> 底层全在（双雷达 `/scan0`+`/scan1`、`/cmd_vel`、`/odom_raw`、`/imu/data_raw`、机械臂、`/beep`、`/battery`），
> **但上位机层没起** —— ❌ 没有 `/tf` `/tf_static`、❌ 没有 `/camera_info`、❌ 没有融合后的 `/odom`。
> 开机自启只带起了手柄那一路。**下一步先搞清楚要手动 launch 哪些节点。**

### 第 1 步 · 机械臂标定 ⭐ 唯一"不做后面全废"的硬依赖

按 `0.机器人配置与操作指引/3.机械臂标定/`，两步：

1. **标中位值** —— 让臂竖直向上 → 关掉通讯代理 → `python3 ~/calibrate_arm.py`
   → 手动把臂掰成一条直线、夹爪完全合拢呈「L」型 → 输入 `y`
2. **标偏移量** —— `ros2 run M3Pro_demo arm_offset`，把 4×4×4 木块机器码放进蓝框 → 空格
3. 最后必须 `cd ~/yahboomcar_ws && colcon build --packages-select arm_kin` 才生效

⚠️ 夹爪角度别乱设：**3 cm 方块 = 134°**，过大会**堵转烧舵机**（尤其 6 号）

**理由**：不做标定，后面所有夹取都是废的；而且它不依赖任何方案决策——
最后用机械臂还是磁吸，标定了都不亏。这是唯一"无脑先做"的事。

### 第 2 步 · 跑官方巡线，量化"视野问题"到底多严重

```bash
ros2 run M3Pro_demo follow_line
```

**记录三个数**：

| 记录项 | 为什么 |
|---|---|
| 能稳定跑多快（`self.linear` 调到多少开始丢线） | 定速度上限 |
| **车前方多少 cm 就看不见线了** | ← **这个数直接决定要不要加灰度阵列** |
| 丢线时的表现（原地打转 / 直冲 / 停下） | 定安全兜底策略 |

➡️ **这个结论出来之前，不要买传感器、不要动硬件。**
现在只有"目测看不清"这个主观判断，需要一个可复现的量化结果。
顺手确认**相机俯角能不能调**——能调的话可能整个加装就省掉了。

---

## 🔴 并行推进（不等车，卡住就亏）

- [ ] **买方块** —— 红/绿/黄/蓝各 2 个，赛前核验、不许含电路
      → 硬物料、**一次性决策**（不像机构可以后期换），**必须同时满足两条路线**：
      ① 内嵌铁片（给磁吸留路）② 外表面平整有夹持面（机械臂能夹）
      ③ 尺寸统一 ④ 颜色饱和度高 ⑤ 不含电路
- [ ] **报名**：填 QQ 群 `1108859083` 公告里的数据收集表（截止 09-30）
- [ ] **问群**：见文末"现在就能问"7 条，一次性问完
- [ ] 从 Windows 取回 2 个小文件 → `resources/`：
      `microROS_STM32-FW_V1.1.2.hex`（控制板出厂固件，控制板出问题时的恢复手段）
      `视觉重定位标签码.pdf`
- [ ] Orin NX 出厂镜像存到 Windows **固定位置** —— 这是车上系统唯一的恢复手段，
      别只躺在下载文件夹里（清理时容易被顺手删掉）

---

## 🟡 第 2 步结论出来之后

### 硬件：底置红外巡线阵列（09-21 实测后决定要加，待量化确认）

> 依据：实测顶部相机做巡线**看不清车前方的地面**（视野主拍远处，脚下有盲区）。
> 详见 `hardware/平台硬件.md` 第 4 节。**是否真加，等第 2 步的数据。**

- [ ] **先确认相机俯角能否调整** —— 若只是装得太仰，调角度可能省掉整个硬件改动
- [ ] **问科协有没有 8 路红外巡线模块**（这类模块很常见，可能一抽屉）
- [ ] 没有则采购：**选红外版，不选可见光灰度版**（现场光照未知，红外不吃可见光）
- [ ] **确认黑色电工胶布实际宽度** ← 仍未知，决定安装高度 / 阵列宽度 / 路口阈值
- [ ] 拍**底盘控制板正反面 + 接口丝印** → 判断有无空闲 IO / I²C / ADC、底部安装空间
- [ ] 定电气接法：阵列 → 小 MCU（RP2040/Nano）→ USB 串口 → Orin（**不动原厂 STM32 固件**）
- [ ] 写巡线节点（`line_error` + `junction_flag`）+ 重调 PID
      ⚠️ 加装后**脱离官方 `follow_line`**，这部分要自己写

### 继续跑通官方例程（`docs/官方例程盘点.md`）

- [x] ~~官方例程盘点~~ → 已完成，结论在 `docs/官方例程盘点.md`
- [ ] 跑通 `1.底盘控制/6.巡线自动驾驶` ← **第 2 步就是在做这个**
- [ ] 跑通 `4.机械臂与三维空间夹取/6.色块颜色分拣`
      （官方 `color_recognize.py` 已内置**红/绿/蓝/黄**四色，与赛题一致）
- [ ] 跑通 `4.机械臂与三维空间夹取/15.巡线清障` ⭐ 最接近赛题的例程
      ```bash
      ros2 launch M3Pro_demo camera_arm_kin.launch.py
      ros2 run   M3Pro_demo grasp_desktop
      ros2 run   M3Pro_demo follow_line
      ```
- [ ] 用 `Roi_hsv()` 在**现场光照下重新标定**黑线与四色
      （`LineFollowHSV.text` 默认 `34,98,74,125,253,255` 是**彩色线**示例值，黑胶布必须重标）

> **这三个跑通之前，不要开始写自己的算法。**

### 然后才需要自己写

- [ ] 定**总体技术路线** → `docs/design/总体方案.md`
- [ ] 定**收集机构方案** → `docs/design/机械与收集机构.md`
  - 约束：宽 ≤21.5cm、高 ≤55cm、单侧余量 9.25cm
  - 官方是"夹一个放一边"；赛题要**带 8 个到出口** → 需携带结构或往返策略
- [ ] 定**软件模块划分** → `docs/architecture/软件架构.md`
- [ ] 官方没给、必须自写的四块：**在线迷宫探索决策 · 多目标收集与携带 · 罚时权衡 · 完整状态机**

### 仿真先行

- [x] 搭 **7×7 迷宫仿真**（通道 40cm、场地 2.8m×2.8m）→ `software/sim/maze_sim.py`（09-23）
- [x] **MazeMap 地图数据结构**（已知/未知路线、frontier、回终点最短路、JSON 可视化）→ `software/src/mazemap.py`（09-23，已与仿真对拍）
- [ ] MazeMap 接进 explore —— 仿真与实车共用同一决策代码
- [ ] 在仿真里验证搜索策略，量化"探索完 7×7 需要多久"
- [ ] 做**罚时敏感性分析**：不同深度方块漏取的代价，倒推"该不该进深支路"

### 误差参数标定（实车日志 → 算法回填，具体算法到时再定）

- [ ] 跑车时**记日志**：里程计 vs 实际位移、转弯指令 vs 实际转角、雷达读数 vs 卷尺实测、巡线横向偏差
- [ ] 用日志算出：里程计比例误差、里程计/雷达噪声 σ、转弯角度误差、跟线横向误差
- [ ] 回填 `software/sim/maze_sim.py` 的占位参数（当前全为猜测值：+3% / σ2cm / σ1cm）

---

## 🟢 中期（10-06 ~ 10-31）

- [ ] 自建实体场地（2.8m×2.8m，挡板高 55cm，黑引导线）→ `field/`
- [ ] 循迹/定位实车调通，跑通直线通道不剐蹭
- [ ] 视觉识别调通（4 色方块，现场光照未知 → 要做过曝/欠曝鲁棒）
- [ ] 收集机构实车验证：能捡起来、能带住不掉
- [ ] 全链路联调：进场 → 探索 → 识别 → 拾取 → 返回出口

---

## 🔵 收尾（11-01 ~ 11-09）

- [ ] 撰写**《设计报告》** → `docs/reports/设计报告.md`（正式交付物，别最后一天写）
- [ ] 尺寸自检：宽度、高度、收纳状态各拍一张照留证
- [ ] 备用方案与容错：卡死怎么办、识别失败怎么办
- [ ] 11-09 20:00 前提交至清水河主楼 A1-304

---

## ❗ 悬而未决（需外部信息）

### 现场才知道（无法提前获知，只能做鲁棒兜底）

| 问题 | 去向 |
|---|---|
| 罚时系数公式 | 现场公布 |
| 迷宫布局 | 现场公布 |
| "小车基准速度"是否可调 | 现场/群内 |
| 出口判定标准（整车/过线） | 群内问 |
| 是否多轮、取最好成绩 | 群内问 |

### 现在就能问（建议一次性问完）

> 复制到群里问科协：
> 1. 高年级组会提供 M3 Pro 赛题的**训练场地或 55 cm 迷宫挡板**吗？平时能预约用吗？
> 2. **迷宫挡板是什么材质？**（是否含铁——若含铁磁性材料，车侧磁吸会被吸在墙上，方案直接出局）
> 3. **黑色电工胶布的实际宽度**是多少？（题面没写，决定传感器安装高度与路口阈值）
> 4. 科协**有没有 8 路红外巡线模块**可借？（09-21 实测相机巡线视野有脚下盲区，需要底置方案兜底）
> 5. Orin NX 上**有没有已配好的 M3 Pro 官方系统镜像/ROS 环境**？还是需要我们自己刷？
> 6. 自备**方块尺寸**有没有限制？（题目只说要核验）
> 7. 报名时间到底以哪个为准 —— 通知第七节写 9-30 23:59，第八节写 11-10？

### 已澄清（不再是缺口）

- ✅ 上位机随箱提供，不用买
- ✅ 两人组队合规（"不多于三人"）
- ✅ 硬件平台完整，A 题 = 二次开发题
- ✅ 网络：手机热点下 `ssh car` 免密已通（走 `yahboom.local` / mDNS）
- ✅ 车上代码已拉到 Mac `car/`（756 个文件受版本管理）

---

## 例会

- 模板：`docs/meetings/例会记录模板.md`
- 每次讨论后新建 `docs/meetings/YYYY-MM-DD.md`，结论回写到对应 design 文档，**不要只留在会议记录里**。

---

## 决策记录

### 2026-09-18 · 要不要提前做代码整合

**结论：不提前做。拿到车、一个一个跑通之后再谈整合。**

理由：① 没硬件无法验证，写完只能"看起来对" ② "整合"的具体含义还没确定（合成一个 launch？还是在某个工作空间加包？只有上车才知道）③ 官方例程本身就是半成品答案，工作是**在它上面改**，不是从零拼 ④ 时间不够做一轮。

**整合的正确时机**：跑通 `巡线自动驾驶` → `色块颜色分拣` → `巡线清障` 之后。
那时是**有依据的减法**，现在是**没依据的加法**。

### 2026-09-21 · 巡线方案改判

**结论：从"官方视觉巡线"改判为"加底置红外阵列"。**

依据：实车实测发现**顶部相机视野主拍远处、脚下有盲区**（视野问题，不是速度问题）。
此前我基于文档推出"官方视觉巡线跑得通"的结论**作废**；顺带作废"先试裁 ROI"的建议
（相机俯角导致主拍远处，裁 ROI 救不了脚下盲区）。

⚠️ 但**先花 5 分钟确认相机俯角能不能调** —— 若只是装得太仰，调角度可能省掉整个硬件加装。

### 2026-09-22 · `car/` 纳入版本管理

**结论：纳入。** 但必须先移除嵌套的 `M3Pro_ws/.git`（Yahboom 内网仓库副本），
否则 Git 只记一个空壳指针，500 个源文件一个都进不去。
官方历史已备份至 `~/M3Pro_ws-官方git历史-备份-20260922.tar.gz`。

**工作流**：改车上代码 → rsync 拉下来 → `git diff` 看改了什么 → commit。
⚠️ rsync 必须加 `--exclude='.git'`，且排除日志要用 `--exclude='/Log/'`（带前导斜杠锚定传输根），
否则 `--exclude=log` 会把 `src/M3Pro_core/openslam_gmapping/log/`（C++ 源码模块）一起干掉。
